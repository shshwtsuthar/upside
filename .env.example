# Database Connection (Get from Supabase Project Settings -> Database -> Connection String URI)
# IMPORTANT: Replace [YOUR-PASSWORD] with your actual Supabase database password!
DATABASE_URL="URL"

# shashwat12345678910
# Google OAuth Credentials (Get from Google Cloud Console)
# See: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="GOOGLE"
GOOGLE_CLIENT_SECRET="GOOGLE"

# NextAuth Secret (Generate a strong random string)
# Use: openssl rand -hex 32 
NEXTAUTH_SECRET="NEXT"
NEXTAUTH_URL="http://localhost:3000" # Important for OAuth redirects in dev

# Custom Encryption Key for Up Token (Generate a 32-byte / 64-hex char key)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
UP_TOKEN_ENCRYPTION_KEY="ENC"
